## Task Overview
Analyze the existing project files (provided as a `.zip`). Apply the required modifications described in the **“Instructions”** section to the Python code.
Be as efficient as possible writing code to use as few AI resources as possible
After completing the changes, generate a new `.zip` file containing the updated code.

---

## Output Requirements
- The final deliverable must be a `.zip` file.
- This `.zip` must include **only the Python (`.py`) files that were modified**.
- Each included file must contain its **full updated code**, not partial snippets.

---

## General Rules

### 1. Modify Only What Is Necessary
- Do not change files unless required by the instructions.
- Do not refactor unrelated code.

### 2. No New Files
- Do not create new files.
- Do not generate documentation files or auxiliary files. Only comment code in-line where needed.

### 3. Exclude Non-Code Files
- Do not modify or include files such as `.md`, `README.md`, or any non-Python files.

### 4. Threading Requirement
- Each major feature or UI tab in the application must run in a **separate thread**.

### 5. Dependencies Between Files
- If instructions requires changes in multiple files, include all affected `.py` files in the output `.zip`.

### 6. Add Logging where needed
- where it makes sense, add logging messages using functions exported by the AppLogging.py module

### 7. Ambiguity Handling
- If any instruction is unclear or incomplete, implement the **most reasonable and robust solution**.
- Do avoid bugs and if a bug is found try to correct it. If instructions are unclear or would introduce a bug on the application, stop the code and .zip file generation and warn me about the situation.
- Consider that this is a **healthcare-focused application** aimed at reducing:
  - Repetitive Strain Injury (RSI)
  - Tendinitis
  - Musculoskeletal Disorders (MSD)

---

## Instructions
    
==========================================
Specifications for RestReminder.py file and functionality expected:

- Existing Application GUI:
  - change rest reminder tab to be the second tab right after the "General" tab
  - this tab (Rest Reminder) will have:
    - at the top a toggle button named "Activate" to activate and deactivate this functionality (which stops/starts the thread for this functionality).
    - one field for each of the environment parameters mentioned on the "functionality logic" section (continuous_work_minutes, delay_pause_minutes, reset_of_work_time_minutes)
  - if any field above is changed, this change should be immediatelly written to the config.ini file using existant modules.
  - on the bottom of the screen display the timers: First, General interaction (elapsed time between variable usage_start_timestamp and last_activity_timestamp) then, mouse interaction (elapsed time between variable usage_start_timestamp and variable last_mouse_activity_timestamp) and last keyboard interaction (elapsed time between variable usage_start_timestamp and last_keyboard_activity_timestamp).


- functionality logic
  - this whole functionality will be a separate thread on the existing application
  - using existing module to write config.ini file, write section [RestReminder] when this module loads with parameters (create config.ini if it does not exist, add parameters below with its defaults if they don't exist):"
    - Active = true
    - continuous_work_minutes = 50 (range = 40 to 120)
    - delay_pause_minutes = 10 (range = 2 to 15)
    - reset_of_work_time_minutes = 5 (range = 1 to 10)
    - rest_time_seconds = 5 (range = 60 to 300)

  - read the environment variables above from config.ini file and activate this functionality thread if active is set to true when module starts and after actions above have been done.
  - Inside this thread maintain the following timers: usage_start_timestamp, last_activity_timestamp, last_keyboard_activity_timestamp, last_mouse_activity_timestamp
  - if this functionality thread just started, first write the current timestamp to the variable usage_start_timestamp and then, after some milliseconds make all 3 variables (last_activity_timestamp, last_keyboard_activity_timestamp, last_mouse_activity_timestamp) have the current timestamp  
  - whenever any key of the keyboard keys is pressed, update last_activity_timestamp and last_keyboard_activity_timestamp with the current timestamp.
  - whenever a mouse button is pressed, update last_activity_timestamp and last_mouse_activity_timestamp with the current timestamp. for mouse inputs, ignore inputs that come from the keyboard keys (mouse press generated by keys F7, F8, F9 and F10 or the ones defined as environment variables on keyboard actions feature)
  - each 2 seconds, monitor the time elapsed between last_activity_timestamp and current timestamp. if it is bigger than variable reset_of_work_time_minutes, reset all timers (first write the current timestamp to the variable usage_start_timestamp and then, after some milliseconds make all 3 variables (last_activity_timestamp, last_keyboard_activity_timestamp, last_mouse_activity_timestamp) have the current timestamp)
  - each 2 seconds also monitor the time elapsed between usage_start_timestamp and last_activity_timestamp. if it is bigger than continuous_work_minutes, show pause screen defined below


  - Pause Screen
    - the pause screen description:
      - should be a window shown in the middle of the main active screen with fixed size and no buttons on the top right to minimize, change size or close.
      - It should contain a friendly phrase encouraging the person to rest for some minutes before continuing work. 
      - Towards the bottom of the screen, there should be a countdown runing. Convert the variable rest_time_seconds in seconds to minutes / seconds and show both counting down on this screen.
      - on the very bottom of the screen there should be 2 buttons. 1= "Dismiss Rest" 2= "Postpone Rest for [delay_pause_minutes] minutes" replacing "[delay_pause_minutes]" on the button's text with the actual minutes on the variable.
      - make the icon of this screen be the same as on the main screen.
      - the title of the screen should be "Rest" or something meaningful
    - as soon as this screen opens, it should take over all computer inputs from keyboard and mouse clicking preventing user from interacting with any other window/app but the pause screen.    
    - Dismiss Rest button action: if clicked, this button should immediatelly release the keyboard and mouse control to the user, reset all timers (first write the current timestamp to the variable usage_start_timestamp and then, after some milliseconds make all 3 variables (last_activity_timestamp, last_keyboard_activity_timestamp, last_mouse_activity_timestamp) have the current timestamp)) and close this pause screen.
    - Postpone Rest button action: when clicked, release the keyboard and mouse control to the user and on the background initiate a countdown timer with the duration of "delay_pause_minutes". when this timer elapses, show the pause screen again with a new countdown as described on how the pouse screen should appear above. The "postpone rest" button should become unavailable if the user clicks it (to postopone) more than 3 times forcing the user to wait or click cancel. while the "delay_pause_minutes" background timer is counting down, temporarily stop checking each 2 seconds if the elapsed time between the last_activity_timestamp and current timestamp is bigger than "continuous_work_minutes" minutes. resume this "each 2 seconds" check if the "pause screen" is closed by any other reason than clicking the "postpone' button.    
    - if the timer on the pause screen elapses (user waited for it to elapse), do: immediatelly release the keyboard and mouse control to the user, reset all timers (first write the current timestamp to the variable usage_start_timestamp and then, after some milliseconds make all 3 variables (last_activity_timestamp, last_keyboard_activity_timestamp, last_mouse_activity_timestamp) have the current timestamp)) and close the pause screen.

    - if anything fails or an exception happens on this function of the application the whole functionality thread is stopped effectively giving the user full control again on keyboard and mouse.
        
==========================================
